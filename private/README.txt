Place talky.zip in this folder for paid downloads.
The app serves it only via GET /download after Stripe checkout verification.
You can override the path with ARCHIVE_PATH in .env (relative to project root or absolute).
